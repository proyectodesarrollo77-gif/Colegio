from django.apps import AppConfig


class AgendaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "core.agenda"
    label = "agenda"
    verbose_name = "Agenda Virtual"
